App As appinfo:
    BackEnabled: =false
    OnStart: =
    StartScreen: |
        =Navigate(scrHomePage)
    Theme: =RedTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

